<%= render "nav" %>
<% if user_signed_in? %>
  <div class="box">
    <ul>
      <% @users.each do |user| %>
        <li><%= link_to "#{user.name}: #{user.email}", user_path(user.id) %></li>
      <% end %>
    </ul>
  </div>
<% end %>

